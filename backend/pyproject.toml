[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[openai,xai]>=1.3.10",
    "python-dotenv>=1.2.1",
]
